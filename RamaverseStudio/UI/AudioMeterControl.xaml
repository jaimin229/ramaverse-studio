<UserControl x:Class="RamaverseStudio.UI.AudioMeterControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="16" d:DesignWidth="240">
    <Grid Height="12" Margin="0,2">
        <Border Background="#0A0A0A" CornerRadius="2" BorderBrush="#222222" BorderThickness="1" ClipToBounds="True">
            <Grid>
                <!-- Monochrome Audio Level Gradient Fill -->
                <Rectangle x:Name="MeterBar" HorizontalAlignment="Left" Width="0">
                    <Rectangle.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#444444" Offset="0.0"/>   <!-- -60dB Deep Silver -->
                            <GradientStop Color="#777777" Offset="0.65"/>  <!-- -18dB Mid Silver -->
                            <GradientStop Color="#BBBBBB" Offset="0.85"/>  <!-- -6dB Bright Silver -->
                            <GradientStop Color="#FFFFFF" Offset="1.0"/>   <!-- 0dB Pure White -->
                        </LinearGradientBrush>
                    </Rectangle.Fill>
                </Rectangle>

                <!-- Peak Hold Line -->
                <Rectangle x:Name="PeakLine" Width="1.5" HorizontalAlignment="Left" Fill="#FFFFFF" Visibility="Collapsed"/>

                <!-- Scale Tick Marks -->
                <Grid IsHitTestVisible="False">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="65*"/> <!-- -18 dB -->
                        <ColumnDefinition Width="20*"/> <!-- -6 dB -->
                        <ColumnDefinition Width="15*"/> <!-- 0 dB -->
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" BorderBrush="#25FFFFFF" BorderThickness="0,0,1,0"/>
                    <Border Grid.Column="1" BorderBrush="#25FFFFFF" BorderThickness="0,0,1,0"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
